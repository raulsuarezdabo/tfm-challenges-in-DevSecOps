# required metadata
sonar.projectKey=tfm-devsecop
sonar.projectName=TFM-DevSecOp
sonar.projectVersion=1.0-SNAPSHOT

# optional description
sonar.projectDescription=TFM: DevSecOp

# path to source directories (required)
sonar.sources=src/main/java

# path to test source directories (optional)
sonar.tests=src/test/java

# path to project binaries (optional), for example directory of Java bytecode
#sonar.binaries=bin

# optional comma-separated list of paths to libraries. Only path to JAR file and path to directory of classes are supported.
#sonar.libraries=path/to/library/*.jar,path/to/classes/dir,path/to/specific/library/myLibrary.jar,parent/*/*.jar

sonar.language=java
sonar.jacoco.reportPaths=target/jacoco.exec
sonar.dependencyCheck.xmlReportPath=${WORKSPACE}/dependency-check-report.xml
#sonar.dependencyCheck.htmlReportPath=target/dependency-check-report.html
sonar.java.binaries=target/classes/
sonar.host.url=http://sonarqube:9000